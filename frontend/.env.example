# Base URL of the Django backend API (no trailing slash, no /api suffix).
# Leave unset for local dev to use the Vite proxy defined in vite.config.ts.
VITE_API_URL=https://study-tracker-pcm7.onrender.com
